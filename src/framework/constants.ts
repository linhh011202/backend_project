export enum HttpMetadata {
  Method = 'HTTP.METHOD',
  Path = 'HTTP.PATH',
  Prefix = 'HTTP.PREFIX',
}

export enum HttpMethod {
  Get = 'get',
  Post = 'post',
  Put = 'put',
  Patch = 'patch',
  Delete = 'delete',
}
